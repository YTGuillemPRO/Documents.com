import pygame from player import Player from world import World from ui import draw_ui pygame.init() # Pantalla WIDTH, HEIGHT = 800, 600 screen = pygame.display.set_mode((WIDTH, HEIGHT)) pygame.display.set_caption("Crónicas del Multiverso") # Reloj clock = pygame.time.Clock() FPS = 60 # Mundo y jugador world = World() player = Player(100, 100) # Loop principal running = True while running: screen.fill((0, 0, 0)) # Fondo negro for event in pygame.event.get(): if event.type == pygame.QUIT: running = False keys = pygame.key.get_pressed() player.update(keys, world) world.draw(screen) player.draw(screen) draw_ui(screen, player) pygame.display.flip() clock.tick(FPS) pygame.quit()
